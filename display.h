#pragma once
#include"solution.h"

void capture(string s);

void draw_cost_iteration(int h, int w,const vector<coordinates>& cost_iteration, int best_loop);

void display_network(int h, int w);


void display_clusters(int w, int h);
